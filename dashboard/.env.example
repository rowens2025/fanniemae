# Neon / Postgres (read-only user recommended)
DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require

# Optional: restrict CORS to your marketing site (comma-separated origins not supported; single origin or omit for *)
# DASHBOARD_CORS_ORIGIN=https://your-site.vercel.app
